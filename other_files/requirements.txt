# ============================================================
# TinyML UWB Multi-Person Localization - dependencies
# Tested with Python 3.11 / 3.12
# ============================================================

# --- Core numerics ---
numpy>=1.26,<3.0
scipy>=1.11

# --- Deep learning ---
# CPU-only: torch>=2.1,<3.0
# GPU (CUDA 12.x): install via: pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.1,<3.0
torchvision>=0.16,<1.0

# --- ONNX export (for MCU deployment pipeline) ---
onnx>=1.15

# --- Notebooks ---
jupyter>=1.0
ipykernel>=6.29
nbformat>=5.10

# --- Plotting ---
matplotlib>=3.8

# --- PDF generation (briefings + training report) ---
reportlab>=4.0
Pillow>=10.0

# --- Progress bars (used in notebooks) ---
tqdm>=4.66
